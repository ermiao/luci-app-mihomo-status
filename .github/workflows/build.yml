name: Build luci-app-mihomo-status (x86_64)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev \
          python3 python3-pip python3-venv unzip zlib1g-dev file wget

    - name: Download OpenWrt SDK (x86_64)
      run: |
        wget https://downloads.openwrt.org/releases/23.05.3/targets/x86/64/openwrt-sdk-23.05.3-x86-64_gcc-12.3.0_musl.Linux-x86_64.tar.xz
        tar -xf openwrt-sdk-*.tar.xz
        SDK_DIR=$(find . -maxdepth 1 -type d -name "openwrt-sdk-*")
        mv "$SDK_DIR" openwrt-sdk

    - name: Add luci-app-mihomo-status to SDK
      run: |
        cp -r ./luci-app-mihomo-status ./openwrt-sdk/package/

    - name: Build luci-app-mihomo-status
      run: |
        cd openwrt-sdk
        ./scripts/feeds update -a || true
        ./scripts/feeds install -a || true
        make defconfig
        make package/luci-app-mihomo-status/compile V=s

    - name: Upload Compiled IPK
      uses: actions/upload-artifact@v4
      with:
        name: luci-app-mihomo-status-ipk
        path: |
          openwrt-sdk/bin/packages/*/*/luci-app-mihomo-status_*.ipk
